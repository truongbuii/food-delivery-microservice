server:
  port: 8085
  servlet:
    context-path: /order

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/food_delivery_order
    username: root
    password: secret
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    database: postgresql
